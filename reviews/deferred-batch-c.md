# Deferred Batch C Changes

The following items were identified in the master plan for Batch C but reside in files not owned by this agent:

- **P32 (Non-semantic HTML):** Convert `snippet-card` and `ctx-overlay` in `ListView.jsx` from `div` to semantic interactive elements (e.g., `<button>` or `<a>`).
- **P34 (Context Menu Focus Trap):** Implement focus trap and focus restoration for `ctx-menu` in `ListView.jsx`.
- **P35 (Keyboard Access):** The master plan mentions kebab menu for keyboard access. This resides in `ListView.jsx`.

These should be handled in Batch B (Frontend Views).
